function Card(cardId, badLuckIndex, situationName, image) {
    this.cardId = cardId;
    this.badLuckIndex = badLuckIndex;
    this.situationName = situationName;
    this.image = image;
}

export default Card;